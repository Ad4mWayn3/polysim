# Polysim

Convex polygon collision detection system and simulator.

Includes implementations of broad-phase methods like
Sweep and Prune, and lazy bruteforce, and narrow-phase methods like
Separating Axis Theorem, and in the future, GJK.

## How to run

With zig version 0.16.0, inside the root folder run:
```
zig build run
```
